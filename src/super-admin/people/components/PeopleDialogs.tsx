import { memo } from 'react';

/**
 * Placeholder container for future People module dialogs:
 * create user, edit user, suspend confirmation, delete confirmation, import/export.
 */
export const PeopleDialogs = memo(function PeopleDialogs() {
  return null;
});

export default PeopleDialogs;
